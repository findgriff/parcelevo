{
  "name": "parcelevo",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "pnpm": {
    "overrides": {
      "fastify": "4.28.1"
    }
  },
  "devDependencies": {
    "@prisma/client": "7.2.0",
    "cross-env": "^7.0.3",
    "prisma": "^7.2.0",
    "ts-node": "^10.9.1"
  }
}
